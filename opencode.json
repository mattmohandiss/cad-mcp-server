{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "cad-mcp-server": {
      "type": "local",
      "command": ["npx", "tsx", "src/index.ts"],
      "enabled": true,
      "timeout": 30000
    }
  }
}
