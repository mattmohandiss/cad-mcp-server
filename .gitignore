# Local/generated project artifacts present in this repo
.direnv/
dist/
node_modules/

# Manual test artifacts
*.stp
*.STEP

# occt-wasm fork build artifacts (rebuild via Docker, not committed)
vendor/occt-wasm/dist/
vendor/occt-wasm/ts/dist/
