# Local/generated project artifacts present in this repo
.direnv/
.opencode/
dist/
node_modules/
*.tgz

# Manual test artifacts
*.stp
*.STEP

# occt-wasm build artifacts (rebuild via just build, not committed)
occt/dist/
occt/ts/dist/
occt/build/
occt/*.tgz
